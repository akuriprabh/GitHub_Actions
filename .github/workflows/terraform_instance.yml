name: Terraform Deploy

on:
  # push:
  #   branches: [ main ]
  workflow_dispatch:   # Manual trigger only

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest

    env:
      # AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      # AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: us-east-1 # change if needed

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::786193448103:role/GitHubActionsTerraformRole
          aws-region: us-east-1

      - name: Test AWS Connection
        run: aws sts get-caller-identity

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan


######################################################
#     OIDC Trust Policy for GitHub Actions.          #
######################################################
# {
# 	"Version": "2012-10-17",
# 	"Statement": [
# 		{
# 			"Effect": "Allow",
# 			"Principal": {
# 				"Federated": "arn:aws:iam::786193448103:oidc-provider/token.actions.githubusercontent.com"
# 			},
# 			"Action": "sts:AssumeRoleWithWebIdentity",
# 			"Condition": {
# 				"StringEquals": {
# 					"token.actions.githubusercontent.com:aud": "sts.amazonaws.com"
# 				},
# 				"StringLike": {
# 					"token.actions.githubusercontent.com:sub": "repo:akuriprabh*/GitHub_Actions*:ref:refs/heads/main"
# 				}
# 			}
# 		}
# 	]
# }
